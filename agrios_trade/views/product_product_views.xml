<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_search_form_view" model="ir.ui.view">
        <field name="name">product.product.search - agrios_trade</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="agrios_product.product_search_form_view"/>
        <field name="mode">primary</field>
        <field name="priority" eval="20"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_input_product']" position="after">
                <separator/>
                <filter string="Input Products" name="filter_input_product" domain="[('input_product','=',True)]"/>
            </xpath>
        </field>
    </record>

</odoo>
